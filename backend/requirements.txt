# ==========================================================
# BHUDRISHTI BACKEND PRODUCTION DEPENDENCIES
# ==========================================================

# Core Web Framework
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Geospatial & Raster Processing (CPU Engine)
numpy>=1.24.0
rasterio>=1.3.0
pyproj>=3.5.0
shapely>=2.0.0
geopandas>=0.13.0
Pillow>=9.5.0
matplotlib>=3.7.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0

# Notes on Environment-Specific & GPU Packages:
# 1. GDAL Python bindings: Best installed via conda (conda install -c conda-forge gdal) or pre-built wheels.
# 2. CuPy (Optional GPU Mosaicing): Install matching your CUDA driver version, e.g.:
#    pip install cupy-cuda11x  # for CUDA 11.x
#    pip install cupy-cuda12x  # for CUDA 12.x
